
@{
    ViewData["Title"] = "SVM";
}

<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Choix de paramètres</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            background-image: url('https://livetechspot.com/wp-content/uploads/2020/07/Complete-Guide-List-on-How-to-Optimize-E-commerce-Website.jpg');
            background-size: cover;
            background-position: center;
            font-family: Arial, sans-serif;
        }

        .container {
            width: 80%;
            margin: 50px auto;
            background-color: rgba(255, 255, 255, 0.8); /* Utiliser une couleur de fond semi-transparente pour une meilleure lisibilité */
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
        }

        h1 {
            text-align: center;
        }

        form {
            max-width: 400px;
            margin: 0 auto;
        }

        label {
            display: block;
            margin-bottom: 10px;
        }

        select {
            width: 100%;
            padding: 10px;
            font-size: 16px;
            margin-bottom: 10px; /* Ajout de marge entre les menus déroulants */
        }

        button {
            background-color: #007bff;
            color: #fff;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
            border-radius: 5px;
            font-size: 16px;
        }

        button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Choisissez les paramètres</h1>
        <form method="post" action="traitement.php">
            <!-- Paramètre 1 -->
            <label for="param1"> Type de noyau:</label>
            <select id="param1" name="param1">
                <option value="max">Maximum</option>
                <option value="min">Minimum</option>
                <option value="ideal">Idéal</option>
            </select>

            <!-- Paramètre 2 -->
            <label for="param2"> Paramètre de régularisation:</label>
            <select id="param2" name="param2">
                <option value="max">Maximum</option>
                <option value="min">Minimum</option>
                <option value="ideal">Idéal</option>
            </select>

            <!-- Paramètre 3 -->
            <label for="param3"> Paramètre de largeur de bande :</label>
            <select id="param3" name="param3">
                <option value="max">Maximum</option>
                <option value="min">Minimum</option>
                <option value="ideal">Idéal</option>
            </select>

            <!-- Paramètre 4 -->
            <label for="param4">Coût des erreurs de classification:</label>
            <select id="param4" name="param4">
                <option value="max">Maximum</option>
                <option value="min">Minimum</option>
                <option value="ideal">Idéal</option>
            </select>

            <br><br>
            <button type="submit">Valider</button>
        </form>
    </div>
</body>
</html>


